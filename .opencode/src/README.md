# Source Area

Implementation lives here only after the spec is defined and approved.

## Rule
- Do not introduce behavior in `src/` that is not traceable to `specs/` and `features/`.

## Usage
- Keep production code here.
- Keep implementation aligned with the active feature spec.
- Keep behavior changes accompanied by verification notes.
